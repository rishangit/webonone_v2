# SMS frontend (Vite) — browser-safe config only (VITE_*)
VITE_API_BASE_URL=http://localhost:4016/api/v1

# Identity peer (login + profile + token exchange)
VITE_IDENTITY_ORIGIN=http://localhost:3011
VITE_IDENTITY_API_BASE_URL=http://localhost:4011/api/v1

# WebOnOne core shell (for platform embed + core nav)
VITE_WEBONONE_ORIGIN=http://localhost:3010

# Allowed iframe parent origins (embed mode)
VITE_ALLOWED_PARENT_ORIGINS=http://localhost:3010,http://localhost:3011,http://localhost:3016
