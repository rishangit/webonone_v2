# Point these at your machine's LAN IP (not localhost) so a physical phone can
# reach the dev backends. Example: http://192.168.1.20:4011
IDENTITY_API_BASE_URL=http://localhost:4011
SMS_API_BASE_URL=http://localhost:4016
