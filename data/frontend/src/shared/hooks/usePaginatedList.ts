/**
 * @deprecated Use `useEpicCatalogList(selectState, actions)` with feature store actions instead.
 * Direct API loaders in pages are no longer supported.
 */
export { useEpicCatalogList as usePaginatedList } from './useEpicCatalogList'
