/** Shared CustomDialog size presets for Data catalog form dialogs (standalone + core-hosted). */
export const DATA_FORM_DIALOG_SIZE = {
  sizeWidth: 'small' as const,
  sizeHeight: 'auto' as const,
}
