VITE_IDENTITY_ORIGIN=http://127.0.0.1:3011
VITE_IDENTITY_API_BASE_URL=http://127.0.0.1:4011/api/v1
VITE_MEDIA_ORIGIN=http://127.0.0.1:3013
VITE_DATA_ORIGIN=http://127.0.0.1:3015
# Optional override. Prefer "Sign in for live demos" on Complex controls —
# showcase stores an Identity JWT and passes it to Select user + Select media
# via postMessage (same parent-owned JWT pattern as WebOnOne).
# VITE_SHOWCASE_ACCESS_TOKEN=
