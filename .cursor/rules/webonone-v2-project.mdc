---
description: WebOnOne v2 scaffold, redirect login, JWT verify, and DB scope
globs: webonone-v2/**/*
alwaysApply: false
---

# WebOnOne v2 project

Agent skill: [.cursor/skills/webonone-agent/SKILL.md](../skills/webonone-agent/SKILL.md)

## Scope

`webonone-v2/frontend`, `webonone-v2/backend`, `webonone-v2/backend/migrations`.

## Ports and env

| Layer | Port | Env file |
|-------|------|----------|
| Frontend | 3000 | `webonone-v2/frontend/.env` |
| Backend | 4000 | `webonone-v2/backend/.env` |

`JWT_SECRET` must match Identity backend. Platform roles (`users_roles`) live in **Identity** — WebOnOne uses `IDENTITY_API_BASE_URL` + `IDENTITY_SERVICE_API_KEY` for role mutations. Super-admin seed: `npm run seed -w @webonone/identity-backend` with `SUPER_ADMIN_USER_ID` in Identity `backend/.env`.

## Database

MySQL schema **`webonone_v2`** — themes, user preferences, site media refs, **companies** (no local `users_roles`). Migrations run via `npm run migrate -w @webonone/webonone-backend`.

## Ownership

- `/login` redirects to Identity — **no local login forms**.
- `/callback` exchanges auth code for JWT via Identity API (standalone OAuth only).
- **All peer redirection** uses the same pattern — see [platform-shell-navigation.mdc](platform-shell-navigation.mdc):
  - **Profile:** `handleProfileClick` + `redirectToIdentityProfile.ts`
  - **Email:** `handleEmailClick` + nav `onClick` on `PLATFORM_EMAIL_NAV` + `redirectToEmail.ts`
  - No `/go/*` routes, no intermediate redirect pages.
- JWT **verification** locally; no per-request Identity HTTP on hot paths.
- Basic Settings (`/settings/basic`) — company registration wizard and company section.
- Company API is **native** to WebOnOne BE (`/api/v1/company/*`) — not a separate microservice.
- **Unified loading overlay** — one AppLayout overlay for session (`SessionRoleGate`), lazy routes (`LazyRoute`), and page fetches (`usePlatformLoading`). See [loading-empty-states.mdc](loading-empty-states.mdc).

Key paths: `features/settings/basic/`, `features/shell/context/PlatformLoadingContext.tsx`, `features/session/components/SessionRoleGate.tsx`, `app/LazyRoute.tsx`, `backend/src/routes/company.routes.ts`, `backend/src/services/company.service.ts`, `backend/src/middleware/auth.ts`, `backend/src/middleware/requireSuperAdmin.ts`.

## Verification

```bash
npm run type-check -w webonone-v2-root
```

Login flow requires Identity running (`npm run dev:identity`).

All interactive UI must use `@webonone/ui-kit` — see [ui-kit-consumption.mdc](ui-kit-consumption.mdc). Do not duplicate UI Kit primitives.

IIS production deploy: [iis-deployment.mdc](../rules/iis-deployment.mdc) · [webonone-v2/deploy/IIS.md](../../webonone-v2/deploy/IIS.md)
