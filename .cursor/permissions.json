{
  // ClickUp pipeline automation — requires Cursor Settings → Agents → Run Mode → Allowlist (or Run Everything).
  // Docs: https://cursor.com/docs/reference/permissions
  "mcpAllowlist": [
    "clickup:*",
    "github:*"
  ],
  "terminalAllowlist": [
    "git",
    "gh",
    "npm",
    "npx",
    "node",
    "yarn",
    "pnpm",
    "cargo",
    "make",
    "python",
    "pip",
    "cd",
    "powershell",
    "pwsh",
    "cmd",
    "tsc",
    "eslint",
    "prettier",
    "curl",
    "wget"
  ],
  "autoRun": {
    "allow_instructions": [
      "During /clickup-spec, /clickup-plan, /clickup-build, /clickup-run, and /clickup-spec-update workflows, allow all shell commands required for git branch work, npm/pnpm builds, type-check, lint, tests, and plan verification in this monorepo.",
      "Allow all ClickUp MCP reads and writes needed to search tasks, fetch descriptions and comments, and batch-update parent and subtask statuses.",
      "Allow GitHub MCP tools when creating branches, pull requests, or checking CI status for spec branches.",
      "Allow WebFetch and WebSearch when gathering external API or documentation references for spec or implementation work."
    ],
    "block_instructions": []
  }
}
