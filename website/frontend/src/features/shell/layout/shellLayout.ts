/** Shell chrome outer inset — matches AppShell (`gap-2 p-2`). */
export const shellChromeRootClassName = 'gap-2 p-2'

/** Gap between main content and right rail inside the shell body row. */
export const shellChromeBodyClassName = 'gap-2'
