VITE_API_BASE_URL=http://127.0.0.1:4018/api/v1
VITE_WEBONONE_ORIGIN=http://127.0.0.1:3010
VITE_IDENTITY_ORIGIN=http://127.0.0.1:3011
VITE_IDENTITY_API_BASE_URL=http://127.0.0.1:4011/api/v1

# AI catalog assistant — guest provider is super-admin/env; signed-in users need WebOnOne Basic Settings → AI
VITE_AI_ORIGIN=http://127.0.0.1:3020
VITE_AI_API_BASE_URL=http://127.0.0.1:4020/api/v1

VITE_SUPPORT_ORIGIN=http://127.0.0.1:3021

# Optional — Map view for catalog search results (same key style as WebOnOne company maps).
# Without this key, List view still works; Map shows an unavailable message.
# VITE_GOOGLE_MAPS_API_KEY=

# Production (IIS): do not copy this file.
# Edit repo-root production.env (from production.env.example), then npm run deploy:<service>
# (runs env:apply → writes backend/.env + frontend/.env.production).
