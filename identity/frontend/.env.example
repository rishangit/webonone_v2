VITE_API_BASE_URL=http://localhost:4001/api/v1

# Production (IIS): copy this file to frontend/.env.production and set https:// URLs before npm run deploy:identity

# Comma-separated redirect URIs for OAuth login and profile return_url origin validation
VITE_ALLOWED_REDIRECT_URIS=http://localhost:3000/callback,http://localhost:3001/profile

# Production (IIS): use https://app.webonone.com/callback in frontend/.env.production
# VITE_ALLOWED_REDIRECT_URIS=https://app.webonone.com/callback,https://identity.webonone.com/profile

VITE_IDENTITY_PROFILE_URL=http://localhost:3001/profile



# Google Sign-In (optional — button hidden when empty)

# 1. Google Cloud Console → APIs & Services → Credentials → Create OAuth 2.0 Client ID (Web application)

# 2. Authorized JavaScript origins: http://localhost:3001 (and production Identity FE origin)

# 3. No redirect URI needed for GIS id_token flow

# 4. Configure OAuth consent screen; add test users if app is in Testing mode

# 5. Copy Client ID here AND set the same value as GOOGLE_CLIENT_ID in backend/.env

VITE_GOOGLE_CLIENT_ID=614019905250-ru7pmllv7of3jq24fnprhs7c0ni9rdgf.apps.googleusercontent.com

