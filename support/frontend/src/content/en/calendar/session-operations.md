---
title: "Session day operations"
category: calendar
slug: session-operations
audience: staff
order: 4
summary: "Start and end a session, then call the next or previous token."
---

On a session day (from the schedule or session screen):

1. **Start** the session when the desk opens.
2. **Call next** (or previous) to move the queue.
3. **End** the session when you close.

Exact buttons depend on the service time mode. Window-mode sessions use [queue tokens](/docs/calendar/queue-token). Duration sessions use [one attendee](/docs/calendar/duration-attendee).
