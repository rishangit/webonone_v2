---
description: Implementation plan for spec 1.6.0 — Company registration with admin approval and platform roles
alwaysApply: false
globs: company/**, webonone-v2/**, ui-kit/package/**
---

# Implementation plan — Spec 1.6.0

## Summary

Spec 1.6.0 adds a **Company microservice** (`company/`) for company registration, membership roles, and super-admin approval. **WebOnOne v2** gets **Basic Settings** (`/settings/basic`) with a **3-step register wizard** (no logo in registration), company status display, and a super-admin pending-approval UI. Company data persists to shared **`webonone_v2`** core schema. See [01-overview.md](./01-overview.md), [02-company-service.md](./02-company-service.md), [03-webonone-company-ui.md](./03-webonone-company-ui.md), [04-super-admin-approval.md](./04-super-admin-approval.md).

## Branch and prerequisites

| Item | Value |
|------|--------|
| Branch | `spec/1.6.0` |
| Migrations | `webonone-v2/backend/migrations/` — companies, memberships, super_admins in `webonone_v2` |
| Env | `company/backend/.env.example` — `DB_NAME=webonone_v2`; `webonone-v2/backend/.env.example` — `COMPANY_API_BASE_URL` |
| JWT | `JWT_SECRET` duplicated in Company + WebOnOne backends (same as Identity) |

---

## Phase 1 — Company service scaffold

**Goal:** Standalone `company/` with health, JWT verify, super-admin seed; tables in `webonone_v2`.

**Exit criteria:** `npm run dev:company` starts; `GET /health` 200; super-admin seed login works.

| Task | Paths |
|------|-------|
| Scaffold service root | `company/package.json`, workspaces |
| Backend Express app | `company/backend/src/` |
| Migrations | `webonone-v2/backend/migrations/` — `companies`, `company_memberships`, `super_admins` |
| Super-admin seed | `company/backend/src/db/seedSuperAdmin.ts` |
| `GET /health` | `company/backend/src/routes/health.ts` |
| `requireIdentityJwt` | `company/backend/src/middleware/requireIdentityJwt.ts` |
| `POST /auth/super-admin/login` | `company/backend/src/routes/auth.ts` |
| Root wiring | Root `package.json` — `dev:company`, workspace |

**Verify:** `curl http://localhost:4004/health`; super-admin login returns token

---

## Phase 2 — Company registration API

**Goal:** Register company and read membership.

**Exit criteria:** `POST /companies` + `GET /me/company` work with Identity JWT.

| Task | Paths |
|------|-------|
| Zod schemas | `company/backend/src/schemas/companySchemas.ts` |
| Repository + service | `company/backend/src/repositories/`, `services/company.service.ts` |
| `POST /companies` | `company/backend/src/routes/company.routes.ts` |
| `GET /me/company` | same |
| One-company-per-user unique constraint | migration |

**Verify:** Manual API test with JWT from Identity login

---

## Phase 3 — Super admin approval API

**Goal:** List pending and approve companies.

**Exit criteria:** Approve sets `approved` + `company_admin` role.

| Task | Paths |
|------|-------|
| `requireSuperAdmin` middleware | `company/backend/src/middleware/requireSuperAdmin.ts` |
| `GET /admin/companies/pending` | `company/backend/src/routes/company.routes.ts` |
| `POST /admin/companies/:id/approve` | same |
| Audit fields | `approved_at`, `approved_by_super_admin_id` |

**Verify:** Approve flow via curl or Postman

---

## Phase 4 — WebOnOne Basic Settings UI

**Goal:** Registration prompt, company states, BE proxy. See [03-webonone-company-ui.md](./03-webonone-company-ui.md).

**Exit criteria:** User completes flow → sees pending status + toast.

| Task | Paths |
|------|-------|
| BE proxy routes | `webonone-v2/backend/src/routes/company.routes.ts` |
| `COMPANY_API_BASE_URL` env | `webonone-v2/backend/.env.example` |
| Nav + route | `navItems.ts`, `router.tsx` |
| `BasicSettingsPage` | `features/settings/basic/pages/BasicSettingsPage.tsx` |
| `RegisterCompanyDialog` + step components | `features/settings/basic/components/` |
| Company API client | `features/settings/basic/services/companyApi.ts` |
| Zod form schemas | frontend + backend validation |

**Verify:** `npm run type-check -w webonone-v2-root`; manual register flow

---

## Phase 5 — Super admin UI in WebOnOne

**Goal:** Super-admin login + pending list + approve. See [04-super-admin-approval.md](./04-super-admin-approval.md).

**Exit criteria:** Seed super admin approves company; user sees approved + admin role.

| Task | Paths |
|------|-------|
| Super-admin login page | `features/settings/basic/pages/SuperAdminLoginPage.tsx` |
| Pending companies page | `features/settings/basic/pages/PendingCompaniesPage.tsx` |
| BE proxy admin routes | `webonone-v2/backend/src/routes/company.routes.ts` |
| Session storage token helper | `features/settings/basic/utils/superAdminSession.ts` |
| Route guard | `router.tsx` |

**Verify:** End-to-end approve flow

---

## Phase 6 — Registration wizard

**Goal:** 3-step register wizard with extended company fields. See [03-webonone-company-ui.md](./03-webonone-company-ui.md) wizard section.

**Exit criteria:** Wizard collects basics, location/contact, summary; full payload persisted; pending flow unchanged.

| Task | Paths |
|------|-------|
| Migration — extend `companies` | `webonone-v2/backend/migrations/` |
| Register Zod schemas | `company/backend/src/schemas/`, `webonone-v2/backend/src/schemas/`, `webonone-v2/frontend/src/features/settings/basic/schemas/` |
| Wizard step components | `webonone-v2/frontend/src/features/settings/basic/components/register-wizard/` |
| `RegisterCompanyDialog` orchestrator | `features/settings/basic/components/RegisterCompanyDialog.tsx` |
| `companyApi.registerCompany` payload | `features/settings/basic/services/companyApi.ts` |

**Verify:** `npm run type-check -w company-root`; `npm run type-check -w webonone-v2-root`; manual wizard flow

---

## Phase 7 — Registration wizard polish

**Goal:** Subtask 86ey2punp — remove logo, fix overlay z-index, labeled Previous/Next nav, country/phone controls, optional address fields.

**Exit criteria:** Wizard usable in dialog; UI Kit country + phone inputs; state/postal optional.

| Task | Paths |
|------|-------|
| Optional `logoUrl`; remove logo from wizard | `company/backend/`, `webonone-v2/frontend/.../basic/` |
| Select/Popover z-index `z-[110]` | `ui-kit/package/src/components/Select.tsx`, `Popover.tsx` |
| Export `CountrySelect` | `ui-kit/package/src/components/CountrySelect.tsx`, `index.ts` |
| Previous/Next with icon + text | `RegisterCompanyDialog.tsx` |
| Country + PhoneInput on step 2 | `RegisterWizardStepLocationContact.tsx` |
| Optional state/postal schemas | FE + `webonone-v2/backend` + `company/backend` schemas |

**Verify:** type-check company + webonone; manual wizard in dialog

---

## Phase 8 — Verification

```bash
npm run type-check -w company-root
npm run type-check -w webonone-v2-root
```

Manual: register company via wizard; pending UI; super-admin approve; approved + company_admin role; Company standalone `/health`; both services on `webonone_v2`.

---

## Cross-service integration

| Channel | Usage |
|---------|--------|
| **JWT** | Identity token on Company user routes; WebOnOne verifies then proxies |
| **HTTP proxy** | WebOnOne BE → Company API (no browser direct call in prod) |

Media logo upload — **deferred** (post-approval edit; `logo_url` column retained).

---

## ClickUp traceability

| ClickUp item | ID | Phase |
|--------------|-----|-------|
| Spec No 1.6.0 Register my company | 86ey2nrgd | Phases 1–8 |
| Core project need to have the user roles | 86ey2p61f | Phases 1–5 |
| comapny registration need to improve | 86ey2pmp2 | Phase 6 |
| comapny regitration improvements | 86ey2punp | Phase 7 |

---

## Risks and open items

| Risk | Mitigation |
|------|------------|
| Shared core DB exception | Document in 01/02; both services must use same `DB_NAME` |
| New service scaffold size | Mirror `media/` layout |
| Super-admin token vs Identity JWT | Separate `aud` claim + session storage |
