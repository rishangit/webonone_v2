---
description: Implementation plan for spec 1.11.0 — Data microservice for tags, UOM, attributes, products, services, spaces
alwaysApply: false
globs: ui-kit/**, data/**, email/**, identity/frontend/**, webonone-v2/frontend/**, media/frontend/**, .cursor/rules/**, .cursor/skills/item-list/**
---

# Implementation plan — Spec 1.11.0

## Summary

Spec 1.11.0 adds a standalone **Data microservice** (`data/`). Phase 8 delta: UI Kit **`LoadingState`** + **`ListEmptyState`** and rollout across services. See [09-ui-kit-loading-empty-states.md](./09-ui-kit-loading-empty-states.md).

## Branch and prerequisites

| Item | Value |
|------|--------|
| Branch | `spec/1.11.0` |
| Ports | FE :3005, BE :4005 |
| DB | `webonone_data` |
| Package builds | `build:platform-nav`, `build:theme`, `build:ui-kit` before data prod build |
| JWT | Same `JWT_SECRET` in data + identity backends |

---

## Phase 1 — Service scaffold

**Goal:** Runnable `data/` with health, auth shell, base migrations.

**Exit criteria:** `npm run dev:data` starts; `GET /health` 200.

| Task | Paths |
|------|-------|
| Root workspace | `package.json` |
| Service root | `data/package.json` |
| Backend scaffold | `data/backend/src/**` (mirror `email/backend`) |
| Frontend scaffold | `data/frontend/src/**` (mirror `email/frontend`) |
| Base migration | `data/backend/migrations/*_data_base_tables.ts` |
| Nav + layout | `data/frontend/src/features/shell/**`, `AppLayout.tsx` |
| Auth callback | `data/frontend/src/features/auth/**` |
| Deploy stub | `data/deploy/IIS.md`, `web.config` |

**Verify:** `npm run migrate -w data-root` · `npm run type-check -w data-root`

---

## Phase 2 — Domain API

**Goal:** CRUD + list APIs for all six entities.

**Exit criteria:** List endpoints return `{ items, total, page, pageSize }`.

| Task | Paths |
|------|-------|
| Junction migration | `data/backend/migrations/*_data_junction_tables.ts` |
| Zod schemas | `data/backend/src/schemas/*.schema.ts` |
| Services | `data/backend/src/services/*.service.ts` |
| Routes | `data/backend/src/routes/*.routes.ts` |
| Auth middleware | `requireSuperAdmin` on POST/PUT/PATCH/DELETE |

**Verify:** curl/manual API smoke per entity

---

## Phase 3 — Admin UI lists

**Goal:** Six list pages with search, filter panel, pagination.

| Task | Paths |
|------|-------|
| API client | `data/frontend/src/shared/services/dataApi.ts` |
| List pages | `data/frontend/src/features/{tags,units,attributes,products,services,spaces}/pages/*Page.tsx` |
| List components | `*/components/*List.tsx` |
| Router | `data/frontend/src/app/router.tsx` + `LazyRoute.tsx` |
| Vite chunks | `data/frontend/vite.config.ts` |

**Verify:** `npm run type-check -w data-root`

---

## Phase 4 — Admin UI editors

**Goal:** Create/edit/delete forms for all entities.

| Task | Paths |
|------|-------|
| Editor pages | `*/pages/*EditorPage.tsx` |
| Zod form schemas | `*/schemas/*.ts` |
| Dashboard | `data/frontend/src/features/dashboard/pages/DashboardPage.tsx` |

**Verify:** Full CRUD smoke in dev

---

## Phase 5 — Platform integration

**Goal:** WebOnOne nav + peer env.

| Task | Paths |
|------|-------|
| Platform nav | `packages/platform-nav/src/coreNav.ts` |
| WebOnOne config | `webonone-v2/frontend/src/features/data/utils/dataConfig.ts` |
| WebOnOne nav hook | `webonone-v2/frontend/src/app/AppLayout.tsx` or shell |
| Env examples | `webonone-v2/frontend/.env.example` |

**Verify:** Data link opens Data origin with handoff

---

## Phase 6 — Documentation

| Task | Paths |
|------|-------|
| AGENTS.md | Data service row |
| Agent stub | `.cursor/agents/data-agent.md`, `.cursor/skills/data-agent/SKILL.md` |

---

## Phase 7 — List page layout refinements (delta)

**Goal:** [08-list-page-layout-refinements.md](./08-list-page-layout-refinements.md).

**Exit criteria:** Data lists match header + pagination layout contract.

| Task | Paths |
|------|-------|
| Move search to header | `data/frontend/src/features/tags/pages/TagsPage.tsx`, `units/...`, `attributes/...`, `catalog/pages/CatalogListPage.tsx` |
| Pagination bottom pin | Same pages — `flex-1` list wrapper + `Pagination className="mt-auto"` |
| Rules | `.cursor/rules/item-list-pagination.mdc`, `list-filter-panel.mdc` |
| Skill | `.cursor/skills/item-list/SKILL.md` |

**Verify:** `npm run type-check -w data-root`

Spec: subtasks **86ey5g15b**, **86ey5g1t4**

---

## Phase 8 — UI Kit loading and empty states (delta)

**Goal:** [09-ui-kit-loading-empty-states.md](./09-ui-kit-loading-empty-states.md).

| Task | Paths |
|------|-------|
| `LoadingState` | `ui-kit/package/src/components/LoadingState.tsx` |
| `ListEmptyState` | `ui-kit/package/src/components/ListEmptyState.tsx` |
| Exports + showcase | `ui-kit/package/src/index.ts`, `ui-kit/showcase/src/pages/ComponentsPage.tsx` |
| Cursor rule | `.cursor/rules/loading-empty-states.mdc`, `README.md` index |
| Item-list skill | `.cursor/skills/item-list/SKILL.md` |
| Data rollout | list pages, editors, dashboard, `LazyRoute`, handoff |
| Email rollout | history, queue, templates, dashboard, editors, `LazyRoute` |
| WebOnOne / Media / Identity | `LazyRoute`, loading pages |

**Verify:** `npm run build -w @webonone/ui-kit` · type-check all touched service roots

Spec: subtasks **86ey5g5r1**, **86ey5g845**

---

## Cross-service integration

| Channel | Use |
|---------|-----|
| Identity redirect | Data FE login via auth code |
| JWT | Data BE verifies locally |
| WebOnOne nav | Redirect to `VITE_DATA_ORIGIN` |
| Public HTTP | Peers read `GET /api/v1/*` with user JWT (documented; no consumer impl required) |

---

## ClickUp traceability

| ClickUp | ID | Phase |
|---------|-----|-------|
| Parent: [User Story] Spec No 1.11.0 Data microservice | 86ey5f6vq | Phases 1–6 |
| Subtask: pagination position | 86ey5g15b | Phase 7 |
| Subtask: search button and the filter button align to right | 86ey5g1t4 | Phase 7 |
| Subtask: need to add the loading component | 86ey5g5r1 | Phase 8 |
| Subtask: need to have the no result found commpn component | 86ey5g845 | Phase 8 |

---

## Risks and open items

- Large scaffold — mirror Email closely to reduce drift.
- Product attribute repeater UX — start with simple add/remove rows.
- Role sync deferred — super_admin only for writes in 1.11.0.
- Company-scoped data reserved for future spec.

---

## Final verification

```bash
npm run build -w @webonone/ui-kit
npm run type-check -w ui-kit-root
npm run type-check -w data-root
npm run type-check -w email-root
npm run type-check -w webonone-v2-root
npm run type-check -w media-root
npm run type-check -w identity-root
```
